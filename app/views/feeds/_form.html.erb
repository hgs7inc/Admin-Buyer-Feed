<%= form_with(model: feed, local: true) do |form| %>
  <% if feed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feed.errors.count, "error") %> prohibited this feed from being saved:</h2>

      <ul>
      <% feed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :article_id %>
    <%= form.text_field :article_id, id: :feed_article_id %>
  </div>

  <div class="field">
    <%= form.label :dailyfeatured_id %>
    <%= form.text_field :dailyfeatured_id, id: :feed_dailyfeatured_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
